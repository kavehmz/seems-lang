# Copy to .env and put your own key here. Never commit a real key.
TYPESAFE_API_KEY=your_typesafe_api_key_here

# Optional. The default is jev-latest. Pin a version (for example jev-1.13.0) to keep tuned levels stable.
# TYPESAFE_MODEL=jev-latest
